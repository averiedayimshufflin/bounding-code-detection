# Optional: OCR.space API key for server-side OCR (more accurate on small text)
# Get a free key at https://ocr.space/ocrapi/freekey
# Without this, the app uses Tesseract.js (browser-based OCR, no key needed)
OCR_SPACE_API_KEY=
